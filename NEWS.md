# tidyGenR News

## tidyGenR 0.8.4 (2025-04-4)

### Improvements
- Clarity in documentation of functions: `dereplicate()`, `reads_track()`, `reads_loci_samples()`.
- include vigettes in built.

## tidyGenR 0.8.3 (2025-03-15)

### Improvements
- Removed unnecessary `suppressWarnings()` and `suppressMessages()`, replacing them with proper condition handling.
- Tests updated.

## tidyGenR 0.8.2 (2025-03-03)

### Initial Release
- Core functions available.

### Improvements
- Biochecked: avoid use of `paste`inside warnings, replace `cat` with `message` for efficient handling, use `system2` instead of `system`.
- clean old `.Rd` files.

### Documentation
- NEWS added.